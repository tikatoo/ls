// Package tikatools implements a number of general-purpose utilities.
// Some of these subpackages are named with abbreviations,
// as they may be used with enough frequency
// that long names might "clutter" the code.

package tikatools
